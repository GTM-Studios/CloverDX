# communications/

- `client/` — comms with the client, split into `emails/`, `slack/`, `calls/`
- `internal/` — GTM Studios-only comms, split into `slack/`, `slack-huddle/`, `calls/`

Mixed threads (both audiences in one conversation) go in `internal/` by default.
